name = "bom-app"
compatibility_date = "2026-01-15"

# Pages uses functions folder automatically.
# D1 binding
[[d1_databases]]
binding = "BOM_DB"
database_name = "bom-db"
database_id = "REPLACE_ME"

# R2 bucket for logo
[[r2_buckets]]
binding = "BOM_R2"
bucket_name = "bom-assets"

[vars]
SESSION_TTL_HOURS = "72"
COOKIE_NAME = "bom_session"
